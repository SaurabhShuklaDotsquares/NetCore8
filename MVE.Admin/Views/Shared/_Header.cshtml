<!-- Navbar -->

<nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link toggle-bar-icon" data-widget="pushmenu" href="#"><i class="fa fa-bars"></i></a>
        </li>
    </ul>
    <!-- Right navbar links (Profile Section)-->
    <ul class="navbar-nav ml-auto">
        <li class="nav-item dropdown custom-snav-list">
            <a class="nav-link" data-toggle="dropdown" href="#">
                @if(CurrentUser==null || CurrentUser.ImageName==null || CurrentUser.ImageName=="")
                {
                    <img src="" class="user-image img-circle elevation-2" alt="User Image" style="height:45px;width:45px;">
                }
                else
                {
                         <img src="@CurrentUser.ImageName.Replace("/images/","/Admin/images/")" class="user-image img-circle elevation-2" alt="User Image" style="height:45px;width:45px;">
                }
            </a>
            <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right top-dropdown-setting">
                @* <a href='@Url.Action("Profile", "AdminUser", new { id = 1})' class="dropdown-item">
                <i class="fa fa-user mr-1"></i> Admin Account Details
                </a>
                <a href='@Url.Action("Profile", "AdminUser", new { id = 1})' class="dropdown-item">
                <i class="fa fa-pencil mr-1"></i> Edit
                </a> *@
                <a href="@SiteKeys.Domain/Settings/AdminPersonalDetails" class="dropdown-item">
                    <i class="fa fa-user mr-1"></i> Admin Personal Details
                </a>
                <a href="@SiteKeys.Domain/AdminUser/manage?type=edit" class="dropdown-item">
                    <i class="fa fa-pencil mr-1"></i> Edit
                </a>
                <a href="@SiteKeys.Domain/ManageNotifications/Index" class="dropdown-item">
                    <i class="fa fa-bell mr-1"></i> Notification <label id="lblcNotificationCount" style="color:red"></label>
                </a>
                <a href="@SiteKeys.Domain/Settings/manage" class="dropdown-item">
                    <i class="fa fa-gear mr-1"></i> Settings
                </a>
                <div class="dropdown-divider"></div>
                <a asp-action="signout" asp-controller="account" class="dropdown-item" id="sign_out_id">
                    <i class="fa fa-sign-out mr-1"></i> Log out
                </a>
            </div>
        </li>
    </ul>
</nav>

<!-- /.navbar -->
